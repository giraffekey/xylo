root =
	(l 0 FILL) : (collect grids)

grids =
	for i in 0..24
		grid (i * 15)

grid angle =
	r (angle) (tt (i) (collect cols))

cols =
	for i in 0..10
		collect (rows i)

rows i =
	for j in 0..10
		t (i * 40 - 200) (j * 40 - 200) (ss 4 (h (120 + i * 10) (sat 1 (l 0.5 SQUARE))))
